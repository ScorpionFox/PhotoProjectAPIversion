using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;
using PhotoProjectAPI.Dataset.VM;
using PhotoProjectAPI.Models;

namespace PhotoProjectAPI.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class UserManagmentController : ControllerBase
    {
        //dla mnie
        //ogarnąć użytkowników, tokeny, role, stworzyć AppDbSeeder, dodać w program.cs JWT, Identity, Tokeny, JWT w appsettings.json
    }
}
